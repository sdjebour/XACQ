# Variables Setup
nargs: 2
types: STR, STR
globals: false, false
arch: x86-64

# Emulator Options
bin: datasets/binaries/strverscmp
binsec: datasets/binsec_ini/ijcai22/nopost/strverscmp.ini

# Expected result
precond: (and (or (alias v0 v1) (valid v0)) (or (alias v0 v1) (valid v1)))

# Language
bias: Valid_X, Alias_X_Y

# Conacq options

