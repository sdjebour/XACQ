# Variables Setup
nargs: 2
types: PTR, UINT
globals: false, false
arch: x86-64

# Emulator Options
bin: datasets/binaries/bzero
binsec: datasets/binsec_ini/ijcai22/nopost/bzero.ini

# Expected result
precond: (or (eq v1 #x00000000) (valid v0))

# Language
bias: Valid_X, Eq_X_0

# Conacq options
