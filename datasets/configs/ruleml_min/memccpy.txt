# Variables Setup
nargs: 4
types: PTR, PTR, INT, UINT
globals: false, false, false, false
arch: x86-64

# Emulator Options
bin: datasets/binaries/memccpy
binsec: datasets/binsec_ini/ijcai22/nopost/memccpy.ini

# Expected result
precond: (and (or (eq v3 #x00000000) (valid v0)) (or (eq v3 #x00000000) (valid v1)))

# Language
bias: Valid_X, Eq_X_0

# Conacq options

