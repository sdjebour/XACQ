# Variables Setup
nargs: 2
types: STR, STR
globals: false, false
arch: x86-64

# Emulator Options
bin: datasets/binaries/strstr
binsec: datasets/binsec_ini/ijcai22/nopost/strstr.ini

# Expected result
precond: (and (or (strleneq v1 #x00000000) (valid v0)) (valid v1))

# Language
bias: Valid_X, StrlenEq_X_0

# Conacq options

