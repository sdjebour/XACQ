# Variables Setup
nargs: 2
types: STR, STR
globals: false, false
arch: x86-64

# Emulator Options
bin: datasets/binaries/strcat
binsec: datasets/binsec_ini/ijcai22/nopost/strcat.ini

# Expected result
precond: (and (valid v0) (valid v1) (or (strleneq v1 #x00000000) (not (overlap v0 v1))))

# Language
bias: Valid_X, Overlap_X_Y, StrlenEq_X_0

# Conacq options
