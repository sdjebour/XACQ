# Variables Setup
nargs: 3
types: PTR, INT, UINT
globals: false, false, false
arch: x86-64

# Emulator Options
bin: datasets/binaries/glibc_memchr
binsec: datasets/binsec_ini/ijcai22/nopost/glibc_memchr.ini

# Expected result
precond: (or (eq v2 #x00000000) (valid v0))

# Language
bias: Valid_X, Eq_X_0

# Conacq options

