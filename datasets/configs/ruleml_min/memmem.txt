# Variables Setup
nargs: 4
types: PTR, UINT, PTR, UINT
globals: false, false, false, false
arch: x86-64

# Emulator Options
bin: datasets/binaries/memmem
binsec: datasets/binsec_ini/ijcai22/nopost/memmem.ini

# Expected result
precond: (and (or (eq v3 #x00000000) (ult v1 v3) (valid v0)) (or (eq v3 #x00000000) (ult v1 v3) (valid v2)))

# Language
bias: Valid_X, Eq_X_0, LT_X_Y

# Conacq options
